apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cluster-addons
  namespace: argocd
spec:
  generators:
  - pullRequest:
      github:
        owner: SamiraCavalcanti
        repo: descomplicando-gitops-no-kubernetes-argocd-linuxtips
        tokenRef:
          secretName: secret-token-github
          key: token
      requeueAfterSeconds: 60
  template:
    metadata:
      name: 'pr-{{.number}}-{{.head_short_sha}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/SamiraCavalcanti/descomplicando-gitops-no-kubernetes-argocd-linuxtips.git
        targetRevision: '{{.head_sha}}'
        path: app-of-apps/
      destination:
        server: https://kubernetes.default.svc
        namespace: 'pr-{{.number}}'
      syncPolicy:
        automated: {}
        syncOptions:
        - CreateNamespace=true